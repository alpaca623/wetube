extends layouts/main
include mixins/videoBlock

block content
    .videos
        h1 video
        each item in videos
            +videoBlock({
                title:item.title,
                views:item.views,
                videoFile:item.videoFile
            })