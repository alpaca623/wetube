extends layouts/main
include mixins/videoBlock

block content
    .search__header
        h2 Searching for : #{searchingBy}
    .search__result
        each item in videos
            +videoBlock({
                title:item.title,
                views:item.views,
                videoFile:item.videoFile
            })
    